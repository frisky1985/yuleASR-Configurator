/**
 * OG image generator (minimal stub).
 * NOTE: original script was never committed to the repo (pre-existing gap);
 * this no-op implementation lets `pnpm build` complete. Add real OG image
 * generation (e.g. via sharp) when required.
 */
console.log("[og] no-op: OG image generation not configured (pre-existing gap)");
